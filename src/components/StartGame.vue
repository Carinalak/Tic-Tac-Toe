<script setup lang="ts">
//import { ref, onMounted, defineProps } from 'vue';
//import { Player } from './models/Player';
/*
interface IPlayerProps {
    playerX: Player | null;
    playerO: Player | null;
    gameStarted: boolean;
}

const props = defineProps<IPlayerProps>();




const playerX = ref<Player | null>(props.playerX);
const playerO = ref<Player | null>(props.playerO);
const currentPlayerIndex = ref<number | null>(null);
const gameStarted = ref(false);
*/
/*

onMounted(() => {
  // Logik för att starta spelet när det är monterat
});
*/
/*
watch(() => props.gameStarted, (newVal) => {
  if (newVal) {
    currentPlayerIndex.value = Math.random() < 0.5 ? 0 : 1;
    gameStarted.value = true;
    startGame();
  }
});*/
/*
const startGame = () => {
  props.currentPlayerIndex.value = Math.random() < 0.5 ? 0 : 1;
  props.gameStarted = true;
  
}*/


</script>

<template>

  </template>

<style scoped>
/* Stilar */
</style>
